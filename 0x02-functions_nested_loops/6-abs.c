#include "holberton.h"

/**
 * print_alphabet_x10 - 10 times
 *
 * Return: Always 0
 */
int _abs(int n)
{
       	if (n > 0)
	{
		return (n);
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		return(n * -1);
	}
}
